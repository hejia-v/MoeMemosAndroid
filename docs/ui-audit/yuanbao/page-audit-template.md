# Yuanbao UI 页面审计模板

用途：把 `C:\Users\Admin\Downloads\GameRip\yuanbao` 的 UI 从“感觉很好”拆成可复用的工程资产，后续用于指导 `MoeMemosAndroid` 的设计系统和页面改造。

建议使用方式：

1. 每个关键页面复制一份本模板。
2. 每个页面至少配 3 张截图：首屏、滚动中、交互态。
3. 动效类内容尽量配短录屏，记录时长和触发条件。
4. 不记录“像不像”，只记录“为什么好看、为什么顺手、怎么实现”。

---

## 1. 基本信息

- 页面名称：
- 页面路径/入口：
- 所属流程：
- 主要目标：
- 目标用户动作：
- 参考截图：
- 参考录屏：

## 2. 第一印象

- 视觉关键词：
- 气质判断：
- 信息密度：
- 主要吸引点：
- 主要风险点：

示例：

- 视觉关键词：轻盈、干净、柔和、层次深
- 气质判断：高完成度、低打扰、强内容聚焦
- 信息密度：中

## 3. 页面骨架

- 顶部区域：
- 主内容区域：
- 底部区域：
- 悬浮元素：
- 背景处理：
- 是否沉浸式：
- 是否存在吸顶/折叠/联动：

建议补一张结构草图，至少标出：

- 顶栏
- 主卡片
- 次级操作区
- 浮层
- 安全区处理

## 4. 组件拆解

页面中的主要组件逐个记录，推荐格式如下。

### 组件 A

- 组件名称：
- 组件用途：
- 组件层级：
- 主要状态：
- 视觉特征：
- 交互特征：
- 可复用性：
- 在 `MoeMemosAndroid` 中对应位置：

需要关注的点：

- 圆角大小
- 内外边距
- 阴影/描边/分割线
- 图标大小和留白
- 标题、副标题、辅助文本的对比关系
- 是否存在按压形变、透明度变化、缩放变化

## 5. 视觉规则

### 5.1 色彩

- 主色：
- 辅助色：
- 强调色：
- 背景色：
- 卡片色：
- 分割/描边色：
- 状态色：

### 5.2 字体

- 大标题：
- 标题：
- 正文：
- 辅助信息：
- 标签/按钮：
- 数字信息：

记录字段：

- 字号
- 字重
- 行高
- 字色
- 字间距

### 5.3 形态

- 基础圆角：
- 大卡片圆角：
- Chip 圆角：
- 按钮圆角：
- 图片裁切风格：
- 阴影风格：
- 模糊风格：

### 5.4 间距

- 页面左右边距：
- 模块上下间距：
- 卡片内边距：
- 文本块间距：
- 图标与文本间距：

## 6. 动效与交互

### 6.1 页面级

- 进入动画：
- 退出动画：
- 转场方向：
- 时长：
- easing：
- 是否跟手：

### 6.2 组件级

- 按压反馈：
- 展开/收起：
- 切换状态：
- 输入聚焦：
- 加载态：
- 空状态：

### 6.3 滚动行为

- 是否有吸顶：
- 顶栏是否渐变/缩放/透明：
- 卡片是否位移/视差：
- 列表边缘是否有缓冲：
- 滚动停止后的校正行为：

### 6.4 键盘和输入

- 输入框聚焦时变化：
- 键盘弹起后的布局变化：
- 工具栏联动：
- 提交反馈：

## 7. 状态设计

请至少记录以下状态是否存在：

- 默认态
- 按压态
- 选中态
- 禁用态
- 加载态
- 空态
- 错误态
- 编辑态
- 提交中
- 成功反馈

对每个关键状态记录：

- 视觉变化：
- 动效变化：
- 文案变化：
- 可感知反馈：

## 8. 实现推测

这里只写工程判断，不写主观感受。

- Compose/原生 View 猜测：
- 可能用到的布局方式：
- 可能用到的动画方式：
- 可能的手势处理：
- 可能的性能风险：
- 在 `MoeMemosAndroid` 中的实现难度：

难度评级建议：

- L：现有 Compose 组件稍改即可
- M：需要新建组件或新动画封装
- H：需要页面结构调整或较多状态重写

## 9. 映射到 MoeMemos

- 对应页面：
- 可直接借鉴的点：
- 不适合照搬的点：
- 需要转译的点：
- 建议优先级：

优先级建议：

- P0：直接影响首页气质或输入手感
- P1：影响一致性，但不阻塞首版
- P2：锦上添花

## 10. 最终结论

- 这一页最值得学的 3 个点：
- 这一页最不值得抄的 3 个点：
- 是否进入第一期复刻：
- 如果进入，先做哪一部分：

---

## 附：快速检查清单

- 有没有“默认 Material 3 组件感”太重的问题
- 有没有通过背景、留白、阴影做出层次
- 有没有统一的圆角体系
- 有没有刻意控制信息密度
- 有没有细小但稳定的反馈动画
- 有没有把高频操作放在自然手势范围内
- 有没有处理好状态切换，不靠生硬闪烁
- 有没有把视觉重点放在用户最关心的信息上
